// auth-ui.js — Thin re-export of the shared casino-game auth UI.
// The canonical module lives at /calculator/js/auth/auth-ui.js. It handles
// both the legacy #authSlot inside bb100's Settings drawer AND any .auth-slot
// elements injected by the global Settings modal.
export * from "../../../../js/auth/auth-ui.js";
